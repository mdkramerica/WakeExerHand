[build]
builder = "NIXPACKS"

[build.nixpacks]
providers = ["node"]

[deploy]
startCommand = "npm run start:production"
healthcheckPath = "/api/assessments"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
